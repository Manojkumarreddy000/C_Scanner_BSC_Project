/* These are maulti line comments
 *
 *
 *
 *
 *
 * End of multi line coments*/


// A Single line comment
//


int first_fn(int a);
int fn_2(int b);
signed int fn_3(int c);











#include<stdio.h>

int nice_fn(int a);


#define Num 50
#define swami really_great

#define sum(a,b) (a+b)

void another_fn(char a[],int b[10][2][],char c[5]);


#define as

#define sai really_really_great

void another_fn(int a)
{sairam;
}

int extra_test()
{
		Aum sri sai ram;
		another_fn(1,1,1);
		nice_fn();
}


int main(int argc,char *argv[])
{
 	static unsigned int a=0;
	int b=10,*d;            	float *****f,**c,*v;
	char asd[2],dgf,h;

	signed long sairam;
    printf("This is to test /*so*/ many things like \" together // and this too /*hi*/ \n");

 extra_test();

	while()
	for(i=0;i<10;i++)
		manoj;

	while(sai)
	{

		for(i='\'';i <121;i++)
			for(i=0;i<fdf;i--)
				manoj;
		manoj;
	}

	while(manoj)
	{

		for(i=0;i<some;i++)
		{
				manopj;
	
			for(i=0;i<fj;i++);
			{
				sharad;
				maoj;
				for(i=0;i<123;i++)
					manoj;
			}
		}
	}
	do
	{

	if(a==2)
	{ jai sairam;
		jai jai sairam;
	  for(;;)
	  {
			 ok;
		 fine;
		manoj is here;

		if(manoj)
			great soul;
		else
			for(i=0;i<n;i++)
				shaarad;

	  }
	}
	else
			{ enter;

			 nice_fn(a);
			if(something twice)
					sairam;
					else
							somethin again;
			}
	}while();

   if(this is true)
		   for(;;this is true)
				   i++;
 else
		 if(this is false)
				 this may be false;
				 else true anyway;

  printf("%d",sum(2,3));
}

static int nice_fn(int a)
{
nice_fn(2);
		another_fn(2,2,2);

		main(2,1);

	}
